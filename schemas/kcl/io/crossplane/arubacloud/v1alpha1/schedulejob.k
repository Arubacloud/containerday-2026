"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Schedulejob:
    r"""
    Schedulejob is the Schema for the Schedulejobs API. Manages an ArubaCloud Scheduled Job — a cron-triggered automation task.

    Attributes
    ----------
    apiVersion : str, default is "arubacloud.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Schedulejob", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ArubacloudCrossplaneIoV1alpha1SchedulejobSpec, default is Undefined, required
        spec
    status : ArubacloudCrossplaneIoV1alpha1SchedulejobStatus, default is Undefined, optional
        status
    """


    apiVersion: "arubacloud.crossplane.io/v1alpha1" = "arubacloud.crossplane.io/v1alpha1"

    kind: "Schedulejob" = "Schedulejob"

    metadata?: v1.ObjectMeta

    spec: ArubacloudCrossplaneIoV1alpha1SchedulejobSpec

    status?: ArubacloudCrossplaneIoV1alpha1SchedulejobStatus


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpec:
    r"""
    SchedulejobSpec defines the desired state of Schedulejob

    Attributes
    ----------
    forProvider : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProvider, default is Undefined, required
        for provider
    initProvider : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProvider

    initProvider?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecProviderConfigRef

    writeConnectionSecretToRef?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecWriteConnectionSecretToRef


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProvider:
    r"""
    arubacloud crossplane io v1alpha1 schedulejob spec for provider

    Attributes
    ----------
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones.
        Region identifier (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center).
    name : str, default is Undefined, optional
        (String) Display name for the scheduled job.
        Display name for the scheduled job.
    projectId : str, default is Undefined, optional
        (String) ID of the project that owns this resource.
        ID of the project that owns this resource.
    projectIdRef : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    properties : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProperties, default is Undefined, optional
        properties
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    """


    location?: str

    name?: str

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDSelector

    properties?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProperties

    tags?: [str]

    timeout?: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderProperties:
    r"""
    (Attributes) Job scheduling and execution configuration. (see below for nested schema)

    Attributes
    ----------
    cron : str, default is Undefined, optional
        field cron format.
        Cron expression defining the job schedule (e.g., `0 * * * *` for hourly). Standard 5-field cron format.
    enabled : bool, default is Undefined, optional
        (Boolean) When true, the job is active and will trigger on schedule.
        When `true`, the job is active and will trigger on schedule.
    executeUntil : str, default is Undefined, optional
        time after which a Recurring job stops executing.
        ISO 8601 date-time after which a `Recurring` job stops executing.
    scheduleAt : str, default is Undefined, optional
        time at which the job executes once .
        ISO 8601 date-time at which the job executes once (required for `OneShot` type).
    scheduleJobType : str, default is Undefined, optional
        (String) Execution mode of the job. Accepted values: OneShot (runs once at schedule_at), Recurring (repeats on a cron schedule).
        Execution mode of the job. Accepted values: `OneShot` (runs once at `schedule_at`), `Recurring` (repeats on a `cron` schedule).
    steps : [ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderPropertiesStepsItems0], default is Undefined, optional
        (Attributes List) Ordered list of API actions executed when the job triggers. (see below for nested schema)
    """


    cron?: str

    enabled?: bool

    executeUntil?: str

    scheduleAt?: str

    scheduleJobType?: str

    steps?: [ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderPropertiesStepsItems0]


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecForProviderPropertiesStepsItems0:
    r"""
    arubacloud crossplane io v1alpha1 schedulejob spec for provider properties steps items0

    Attributes
    ----------
    actionUri : str, default is Undefined, optional
        (String) URI of the API action to invoke on the target resource.
        URI of the API action to invoke on the target resource.
    body : str, default is Undefined, optional
        (String) Optional JSON request body sent with the HTTP call.
        Optional JSON request body sent with the HTTP call.
    httpVerb : str, default is Undefined, optional
        (String) HTTP method used to call the action URI (e.g., GET, POST, PUT, DELETE).
        HTTP method used to call the action URI (e.g., `GET`, `POST`, `PUT`, `DELETE`).
    name : str, default is Undefined, optional
        (String) Display name for the scheduled job.
        Optional human-readable label for the step.
    resourceUri : str, default is Undefined, optional
        (String) URI of the ArubaCloud resource that the step targets.
        URI of the ArubaCloud resource that the step targets.
    """


    actionUri?: str

    body?: str

    httpVerb?: str

    name?: str

    resourceUri?: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones.
        Region identifier (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center).
    name : str, default is Undefined, optional
        (String) Display name for the scheduled job.
        Display name for the scheduled job.
    projectId : str, default is Undefined, optional
        (String) ID of the project that owns this resource.
        ID of the project that owns this resource.
    projectIdRef : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    properties : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProperties, default is Undefined, optional
        properties
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    """


    location?: str

    name?: str

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDSelector

    properties?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProperties

    tags?: [str]

    timeout?: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderProperties:
    r"""
    (Attributes) Job scheduling and execution configuration. (see below for nested schema)

    Attributes
    ----------
    cron : str, default is Undefined, optional
        field cron format.
        Cron expression defining the job schedule (e.g., `0 * * * *` for hourly). Standard 5-field cron format.
    enabled : bool, default is Undefined, optional
        (Boolean) When true, the job is active and will trigger on schedule.
        When `true`, the job is active and will trigger on schedule.
    executeUntil : str, default is Undefined, optional
        time after which a Recurring job stops executing.
        ISO 8601 date-time after which a `Recurring` job stops executing.
    scheduleAt : str, default is Undefined, optional
        time at which the job executes once .
        ISO 8601 date-time at which the job executes once (required for `OneShot` type).
    scheduleJobType : str, default is Undefined, optional
        (String) Execution mode of the job. Accepted values: OneShot (runs once at schedule_at), Recurring (repeats on a cron schedule).
        Execution mode of the job. Accepted values: `OneShot` (runs once at `schedule_at`), `Recurring` (repeats on a `cron` schedule).
    steps : [ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderPropertiesStepsItems0], default is Undefined, optional
        (Attributes List) Ordered list of API actions executed when the job triggers. (see below for nested schema)
    """


    cron?: str

    enabled?: bool

    executeUntil?: str

    scheduleAt?: str

    scheduleJobType?: str

    steps?: [ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderPropertiesStepsItems0]


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecInitProviderPropertiesStepsItems0:
    r"""
    arubacloud crossplane io v1alpha1 schedulejob spec init provider properties steps items0

    Attributes
    ----------
    actionUri : str, default is Undefined, optional
        (String) URI of the API action to invoke on the target resource.
        URI of the API action to invoke on the target resource.
    body : str, default is Undefined, optional
        (String) Optional JSON request body sent with the HTTP call.
        Optional JSON request body sent with the HTTP call.
    httpVerb : str, default is Undefined, optional
        (String) HTTP method used to call the action URI (e.g., GET, POST, PUT, DELETE).
        HTTP method used to call the action URI (e.g., `GET`, `POST`, `PUT`, `DELETE`).
    name : str, default is Undefined, optional
        (String) Display name for the scheduled job.
        Optional human-readable label for the step.
    resourceUri : str, default is Undefined, optional
        (String) URI of the ArubaCloud resource that the step targets.
        URI of the ArubaCloud resource that the step targets.
    """


    actionUri?: str

    body?: str

    httpVerb?: str

    name?: str

    resourceUri?: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobStatus:
    r"""
    SchedulejobStatus defines the observed state of Schedulejob.

    Attributes
    ----------
    atProvider : ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [ArubacloudCrossplaneIoV1alpha1SchedulejobStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    lastHandledReconcileAt : str, default is Undefined, optional
        LastHandledReconcileAt holds the value of the most recent
        reconcile-requested-at annotation token that the controller has
        processed. Users can compare this to the annotation to determine
        whether a reconcile request has been handled.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProvider

    conditions?: [ArubacloudCrossplaneIoV1alpha1SchedulejobStatusConditionsItems0]

    lastHandledReconcileAt?: str

    observedGeneration?: int


schema ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProvider:
    r"""
    arubacloud crossplane io v1alpha1 schedulejob status at provider

    Attributes
    ----------
    id : str, default is Undefined, optional
        (String) Computed by the API. Unique identifier for the resource.
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones.
        Region identifier (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center).
    name : str, default is Undefined, optional
        (String) Display name for the scheduled job.
        Display name for the scheduled job.
    projectId : str, default is Undefined, optional
        (String) ID of the project that owns this resource.
        ID of the project that owns this resource.
    properties : ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProviderProperties, default is Undefined, optional
        properties
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    uri : str, default is Undefined, optional
        (String) Computed by the API. Full resource URI used as a reference value in other resources.
        Computed by the API. Full resource URI used as a reference value in other resources.
    """


    id?: str

    location?: str

    name?: str

    projectId?: str

    properties?: ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProviderProperties

    tags?: [str]

    timeout?: str

    uri?: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProviderProperties:
    r"""
    (Attributes) Job scheduling and execution configuration. (see below for nested schema)

    Attributes
    ----------
    cron : str, default is Undefined, optional
        field cron format.
        Cron expression defining the job schedule (e.g., `0 * * * *` for hourly). Standard 5-field cron format.
    enabled : bool, default is Undefined, optional
        (Boolean) When true, the job is active and will trigger on schedule.
        When `true`, the job is active and will trigger on schedule.
    executeUntil : str, default is Undefined, optional
        time after which a Recurring job stops executing.
        ISO 8601 date-time after which a `Recurring` job stops executing.
    scheduleAt : str, default is Undefined, optional
        time at which the job executes once .
        ISO 8601 date-time at which the job executes once (required for `OneShot` type).
    scheduleJobType : str, default is Undefined, optional
        (String) Execution mode of the job. Accepted values: OneShot (runs once at schedule_at), Recurring (repeats on a cron schedule).
        Execution mode of the job. Accepted values: `OneShot` (runs once at `schedule_at`), `Recurring` (repeats on a `cron` schedule).
    steps : [ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProviderPropertiesStepsItems0], default is Undefined, optional
        (Attributes List) Ordered list of API actions executed when the job triggers. (see below for nested schema)
    """


    cron?: str

    enabled?: bool

    executeUntil?: str

    scheduleAt?: str

    scheduleJobType?: str

    steps?: [ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProviderPropertiesStepsItems0]


schema ArubacloudCrossplaneIoV1alpha1SchedulejobStatusAtProviderPropertiesStepsItems0:
    r"""
    arubacloud crossplane io v1alpha1 schedulejob status at provider properties steps items0

    Attributes
    ----------
    actionUri : str, default is Undefined, optional
        (String) URI of the API action to invoke on the target resource.
        URI of the API action to invoke on the target resource.
    body : str, default is Undefined, optional
        (String) Optional JSON request body sent with the HTTP call.
        Optional JSON request body sent with the HTTP call.
    httpVerb : str, default is Undefined, optional
        (String) HTTP method used to call the action URI (e.g., GET, POST, PUT, DELETE).
        HTTP method used to call the action URI (e.g., `GET`, `POST`, `PUT`, `DELETE`).
    name : str, default is Undefined, optional
        (String) Display name for the scheduled job.
        Optional human-readable label for the step.
    resourceUri : str, default is Undefined, optional
        (String) URI of the ArubaCloud resource that the step targets.
        URI of the ArubaCloud resource that the step targets.
    """


    actionUri?: str

    body?: str

    httpVerb?: str

    name?: str

    resourceUri?: str


schema ArubacloudCrossplaneIoV1alpha1SchedulejobStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


