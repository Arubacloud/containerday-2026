"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Subnet:
    r"""
    Subnet is the Schema for the Subnets API. Manages an ArubaCloud Subnet within a VPC. Subnets can be Basic (default networking) or Advanced (custom CIDR with configurable DHCP).

    Attributes
    ----------
    apiVersion : str, default is "arubacloud.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Subnet", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ArubacloudCrossplaneIoV1alpha1SubnetSpec, default is Undefined, required
        spec
    status : ArubacloudCrossplaneIoV1alpha1SubnetStatus, default is Undefined, optional
        status
    """


    apiVersion: "arubacloud.crossplane.io/v1alpha1" = "arubacloud.crossplane.io/v1alpha1"

    kind: "Subnet" = "Subnet"

    metadata?: v1.ObjectMeta

    spec: ArubacloudCrossplaneIoV1alpha1SubnetSpec

    status?: ArubacloudCrossplaneIoV1alpha1SubnetStatus


schema ArubacloudCrossplaneIoV1alpha1SubnetSpec:
    r"""
    SubnetSpec defines the desired state of Subnet

    Attributes
    ----------
    forProvider : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProvider, default is Undefined, required
        for provider
    initProvider : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : ArubacloudCrossplaneIoV1alpha1SubnetSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : ArubacloudCrossplaneIoV1alpha1SubnetSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProvider

    initProvider?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: ArubacloudCrossplaneIoV1alpha1SubnetSpecProviderConfigRef

    writeConnectionSecretToRef?: ArubacloudCrossplaneIoV1alpha1SubnetSpecWriteConnectionSecretToRef


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProvider:
    r"""
    arubacloud crossplane io v1alpha1 subnet spec for provider

    Attributes
    ----------
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier for the resource (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the subnet.
        Display name for the subnet.
    network : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetwork, default is Undefined, optional
        network
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    projectIdRef : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    $type : str, default is Undefined, optional
        created.)
        Subnet type. Accepted values: `Basic` (no custom CIDR), `Advanced` (requires the `network` block). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    vpcId : str, default is Undefined, optional
        created.)
        ID of the parent VPC this subnet belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    vpcIdRef : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDRef, default is Undefined, optional
        vpc Id ref
    vpcIdSelector : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDSelector, default is Undefined, optional
        vpc Id selector
    """


    location?: str

    name?: str

    network?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetwork

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDSelector

    tags?: [str]

    timeout?: str

    $type?: str

    vpcId?: str

    vpcIdRef?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDRef

    vpcIdSelector?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDSelector


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetwork:
    r"""
    (Attributes) Network configuration block. Required when type is Advanced. (see below for nested schema)

    Attributes
    ----------
    address : str, default is Undefined, optional
        1918 notation (e.g., 10.0.1.0/24). Must fall within the parent VPC CIDR.
        Subnet CIDR in RFC-1918 notation (e.g., `10.0.1.0/24`). Must fall within the parent VPC CIDR.
    dhcp : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcp, default is Undefined, optional
        dhcp
    """


    address?: str

    dhcp?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcp


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcp:
    r"""
    (Attributes) DHCP configuration for the subnet. (see below for nested schema)

    Attributes
    ----------
    dns : [str], default is Undefined, optional
        (List of String) List of DNS server IP addresses distributed to DHCP clients.
        List of DNS server IP addresses distributed to DHCP clients.
    enabled : bool, default is Undefined, optional
        (Boolean) Whether DHCP is enabled on this subnet.
        Whether DHCP is enabled on this subnet.
    range : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcpRange, default is Undefined, optional
        range
    routes : [ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcpRoutesItems0], default is Undefined, optional
        (Attributes List) Static routes distributed to DHCP clients. (see below for nested schema)
    """


    dns?: [str]

    enabled?: bool

    range?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcpRange

    routes?: [ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcpRoutesItems0]


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcpRange:
    r"""
    (Attributes) IP address range allocated to DHCP clients. (see below for nested schema)

    Attributes
    ----------
    count : float, default is Undefined, optional
        (Number) Number of consecutive IP addresses in the DHCP pool.
        Number of consecutive IP addresses in the DHCP pool.
    start : str, default is Undefined, optional
        (String) First IP address in the DHCP allocation range.
        First IP address in the DHCP allocation range.
    """


    count?: float

    start?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderNetworkDhcpRoutesItems0:
    r"""
    arubacloud crossplane io v1alpha1 subnet spec for provider network dhcp routes items0

    Attributes
    ----------
    address : str, default is Undefined, optional
        1918 notation (e.g., 10.0.1.0/24). Must fall within the parent VPC CIDR.
        Destination network in CIDR notation. Must be within the subnet's `network.address` CIDR block (e.g., `10.0.1.128/25` when the subnet is `10.0.1.0/24`).
    gateway : str, default is Undefined, optional
        (String) Gateway IP address for this route.
        Gateway IP address for this route.
    """


    address?: str

    gateway?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDRef:
    r"""
    Reference to a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDSelector:
    r"""
    Selector for a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecForProviderVpcIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier for the resource (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the subnet.
        Display name for the subnet.
    network : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetwork, default is Undefined, optional
        network
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    projectIdRef : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    $type : str, default is Undefined, optional
        created.)
        Subnet type. Accepted values: `Basic` (no custom CIDR), `Advanced` (requires the `network` block). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    vpcId : str, default is Undefined, optional
        created.)
        ID of the parent VPC this subnet belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    vpcIdRef : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDRef, default is Undefined, optional
        vpc Id ref
    vpcIdSelector : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDSelector, default is Undefined, optional
        vpc Id selector
    """


    location?: str

    name?: str

    network?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetwork

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDSelector

    tags?: [str]

    timeout?: str

    $type?: str

    vpcId?: str

    vpcIdRef?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDRef

    vpcIdSelector?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDSelector


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetwork:
    r"""
    (Attributes) Network configuration block. Required when type is Advanced. (see below for nested schema)

    Attributes
    ----------
    address : str, default is Undefined, optional
        1918 notation (e.g., 10.0.1.0/24). Must fall within the parent VPC CIDR.
        Subnet CIDR in RFC-1918 notation (e.g., `10.0.1.0/24`). Must fall within the parent VPC CIDR.
    dhcp : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcp, default is Undefined, optional
        dhcp
    """


    address?: str

    dhcp?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcp


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcp:
    r"""
    (Attributes) DHCP configuration for the subnet. (see below for nested schema)

    Attributes
    ----------
    dns : [str], default is Undefined, optional
        (List of String) List of DNS server IP addresses distributed to DHCP clients.
        List of DNS server IP addresses distributed to DHCP clients.
    enabled : bool, default is Undefined, optional
        (Boolean) Whether DHCP is enabled on this subnet.
        Whether DHCP is enabled on this subnet.
    range : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcpRange, default is Undefined, optional
        range
    routes : [ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcpRoutesItems0], default is Undefined, optional
        (Attributes List) Static routes distributed to DHCP clients. (see below for nested schema)
    """


    dns?: [str]

    enabled?: bool

    range?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcpRange

    routes?: [ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcpRoutesItems0]


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcpRange:
    r"""
    (Attributes) IP address range allocated to DHCP clients. (see below for nested schema)

    Attributes
    ----------
    count : float, default is Undefined, optional
        (Number) Number of consecutive IP addresses in the DHCP pool.
        Number of consecutive IP addresses in the DHCP pool.
    start : str, default is Undefined, optional
        (String) First IP address in the DHCP allocation range.
        First IP address in the DHCP allocation range.
    """


    count?: float

    start?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderNetworkDhcpRoutesItems0:
    r"""
    arubacloud crossplane io v1alpha1 subnet spec init provider network dhcp routes items0

    Attributes
    ----------
    address : str, default is Undefined, optional
        1918 notation (e.g., 10.0.1.0/24). Must fall within the parent VPC CIDR.
        Destination network in CIDR notation. Must be within the subnet's `network.address` CIDR block (e.g., `10.0.1.128/25` when the subnet is `10.0.1.0/24`).
    gateway : str, default is Undefined, optional
        (String) Gateway IP address for this route.
        Gateway IP address for this route.
    """


    address?: str

    gateway?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDRef:
    r"""
    Reference to a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDSelector:
    r"""
    Selector for a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecInitProviderVpcIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema ArubacloudCrossplaneIoV1alpha1SubnetSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema ArubacloudCrossplaneIoV1alpha1SubnetStatus:
    r"""
    SubnetStatus defines the observed state of Subnet.

    Attributes
    ----------
    atProvider : ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [ArubacloudCrossplaneIoV1alpha1SubnetStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    lastHandledReconcileAt : str, default is Undefined, optional
        LastHandledReconcileAt holds the value of the most recent
        reconcile-requested-at annotation token that the controller has
        processed. Users can compare this to the annotation to determine
        whether a reconcile request has been handled.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProvider

    conditions?: [ArubacloudCrossplaneIoV1alpha1SubnetStatusConditionsItems0]

    lastHandledReconcileAt?: str

    observedGeneration?: int


schema ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProvider:
    r"""
    arubacloud crossplane io v1alpha1 subnet status at provider

    Attributes
    ----------
    id : str, default is Undefined, optional
        (String) Computed by the API. Unique identifier for the resource.
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier for the resource (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the subnet.
        Display name for the subnet.
    network : ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetwork, default is Undefined, optional
        network
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    $type : str, default is Undefined, optional
        created.)
        Subnet type. Accepted values: `Basic` (no custom CIDR), `Advanced` (requires the `network` block). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    uri : str, default is Undefined, optional
        (String) Computed by the API. Full resource URI used as a reference value in other resources (e.g., as a *_uri_ref attribute).
        Computed by the API. Full resource URI used as a reference value in other resources (e.g., as a `*_uri_ref` attribute).
    vpcId : str, default is Undefined, optional
        created.)
        ID of the parent VPC this subnet belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    """


    id?: str

    location?: str

    name?: str

    network?: ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetwork

    projectId?: str

    tags?: [str]

    timeout?: str

    $type?: str

    uri?: str

    vpcId?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetwork:
    r"""
    (Attributes) Network configuration block. Required when type is Advanced. (see below for nested schema)

    Attributes
    ----------
    address : str, default is Undefined, optional
        1918 notation (e.g., 10.0.1.0/24). Must fall within the parent VPC CIDR.
        Subnet CIDR in RFC-1918 notation (e.g., `10.0.1.0/24`). Must fall within the parent VPC CIDR.
    dhcp : ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcp, default is Undefined, optional
        dhcp
    """


    address?: str

    dhcp?: ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcp


schema ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcp:
    r"""
    (Attributes) DHCP configuration for the subnet. (see below for nested schema)

    Attributes
    ----------
    dns : [str], default is Undefined, optional
        (List of String) List of DNS server IP addresses distributed to DHCP clients.
        List of DNS server IP addresses distributed to DHCP clients.
    enabled : bool, default is Undefined, optional
        (Boolean) Whether DHCP is enabled on this subnet.
        Whether DHCP is enabled on this subnet.
    range : ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcpRange, default is Undefined, optional
        range
    routes : [ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcpRoutesItems0], default is Undefined, optional
        (Attributes List) Static routes distributed to DHCP clients. (see below for nested schema)
    """


    dns?: [str]

    enabled?: bool

    range?: ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcpRange

    routes?: [ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcpRoutesItems0]


schema ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcpRange:
    r"""
    (Attributes) IP address range allocated to DHCP clients. (see below for nested schema)

    Attributes
    ----------
    count : float, default is Undefined, optional
        (Number) Number of consecutive IP addresses in the DHCP pool.
        Number of consecutive IP addresses in the DHCP pool.
    start : str, default is Undefined, optional
        (String) First IP address in the DHCP allocation range.
        First IP address in the DHCP allocation range.
    """


    count?: float

    start?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetStatusAtProviderNetworkDhcpRoutesItems0:
    r"""
    arubacloud crossplane io v1alpha1 subnet status at provider network dhcp routes items0

    Attributes
    ----------
    address : str, default is Undefined, optional
        1918 notation (e.g., 10.0.1.0/24). Must fall within the parent VPC CIDR.
        Destination network in CIDR notation. Must be within the subnet's `network.address` CIDR block (e.g., `10.0.1.128/25` when the subnet is `10.0.1.0/24`).
    gateway : str, default is Undefined, optional
        (String) Gateway IP address for this route.
        Gateway IP address for this route.
    """


    address?: str

    gateway?: str


schema ArubacloudCrossplaneIoV1alpha1SubnetStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


