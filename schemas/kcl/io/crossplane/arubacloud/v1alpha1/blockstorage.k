"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Blockstorage:
    r"""
    Blockstorage is the Schema for the Blockstorages API. Manages an ArubaCloud Block Storage volume.

    Attributes
    ----------
    apiVersion : str, default is "arubacloud.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Blockstorage", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ArubacloudCrossplaneIoV1alpha1BlockstorageSpec, default is Undefined, required
        spec
    status : ArubacloudCrossplaneIoV1alpha1BlockstorageStatus, default is Undefined, optional
        status
    """


    apiVersion: "arubacloud.crossplane.io/v1alpha1" = "arubacloud.crossplane.io/v1alpha1"

    kind: "Blockstorage" = "Blockstorage"

    metadata?: v1.ObjectMeta

    spec: ArubacloudCrossplaneIoV1alpha1BlockstorageSpec

    status?: ArubacloudCrossplaneIoV1alpha1BlockstorageStatus


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpec:
    r"""
    BlockstorageSpec defines the desired state of Blockstorage

    Attributes
    ----------
    forProvider : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProvider, default is Undefined, required
        for provider
    initProvider : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProvider

    initProvider?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecProviderConfigRef

    writeConnectionSecretToRef?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecWriteConnectionSecretToRef


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProvider:
    r"""
    arubacloud crossplane io v1alpha1 blockstorage spec for provider

    Attributes
    ----------
    billingPeriod : str, default is Undefined, optional
        (String) Billing cycle. Accepted values: Hour, Month, Year.
        Billing cycle. Accepted values: `Hour`, `Month`, `Year`.
    bootable : bool, default is Undefined, optional
        (Boolean) Whether this volume can be used as a boot volume for an arubacloud_cloudserver. Must be true when image is set.
        Whether this volume can be used as a boot volume for an `arubacloud_cloudserver`. Must be `true` when `image` is set.
    image : str, default is Undefined, optional
        (String) Image ID to use when creating a bootable volume. Required when bootable is true. See the available images.
        Image ID to use when creating a bootable volume. Required when `bootable` is `true`. See the [available images](https://api.arubacloud.com/docs/metadata/#cloud-server-bootvolume).
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the block storage volume.
        Display name for the block storage volume.
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    projectIdRef : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    sizeGb : float, default is Undefined, optional
        created, because the API rejects a smaller size (Validation: Size: invalid). A replacement discards the volume's contents — check the plan before applying.
        Size of the block storage volume in GiB. Must be a positive integer. Growing a volume is applied in place; **shrinking forces the volume to be destroyed and re-created**, because the API rejects a smaller size (`Validation: Size: invalid`). A replacement discards the volume's contents — check the plan before applying.
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    $type : str, default is Undefined, optional
        created.)
        Storage type. Accepted values: `Standard`, `Performance`. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    zone : str, default is Undefined, optional
        (String) Availability zone within the region. If omitted the volume is regional (accessible across all zones).
        Availability zone within the region. If omitted the volume is regional (accessible across all zones).
    """


    billingPeriod?: str

    bootable?: bool

    image?: str

    location?: str

    name?: str

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDSelector

    sizeGb?: float

    tags?: [str]

    timeout?: str

    $type?: str

    zone?: str


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecForProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    billingPeriod : str, default is Undefined, optional
        (String) Billing cycle. Accepted values: Hour, Month, Year.
        Billing cycle. Accepted values: `Hour`, `Month`, `Year`.
    bootable : bool, default is Undefined, optional
        (Boolean) Whether this volume can be used as a boot volume for an arubacloud_cloudserver. Must be true when image is set.
        Whether this volume can be used as a boot volume for an `arubacloud_cloudserver`. Must be `true` when `image` is set.
    image : str, default is Undefined, optional
        (String) Image ID to use when creating a bootable volume. Required when bootable is true. See the available images.
        Image ID to use when creating a bootable volume. Required when `bootable` is `true`. See the [available images](https://api.arubacloud.com/docs/metadata/#cloud-server-bootvolume).
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the block storage volume.
        Display name for the block storage volume.
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    projectIdRef : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    sizeGb : float, default is Undefined, optional
        created, because the API rejects a smaller size (Validation: Size: invalid). A replacement discards the volume's contents — check the plan before applying.
        Size of the block storage volume in GiB. Must be a positive integer. Growing a volume is applied in place; **shrinking forces the volume to be destroyed and re-created**, because the API rejects a smaller size (`Validation: Size: invalid`). A replacement discards the volume's contents — check the plan before applying.
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    $type : str, default is Undefined, optional
        created.)
        Storage type. Accepted values: `Standard`, `Performance`. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    zone : str, default is Undefined, optional
        (String) Availability zone within the region. If omitted the volume is regional (accessible across all zones).
        Availability zone within the region. If omitted the volume is regional (accessible across all zones).
    """


    billingPeriod?: str

    bootable?: bool

    image?: str

    location?: str

    name?: str

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDSelector

    sizeGb?: float

    tags?: [str]

    timeout?: str

    $type?: str

    zone?: str


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecInitProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema ArubacloudCrossplaneIoV1alpha1BlockstorageSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema ArubacloudCrossplaneIoV1alpha1BlockstorageStatus:
    r"""
    BlockstorageStatus defines the observed state of Blockstorage.

    Attributes
    ----------
    atProvider : ArubacloudCrossplaneIoV1alpha1BlockstorageStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [ArubacloudCrossplaneIoV1alpha1BlockstorageStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    lastHandledReconcileAt : str, default is Undefined, optional
        LastHandledReconcileAt holds the value of the most recent
        reconcile-requested-at annotation token that the controller has
        processed. Users can compare this to the annotation to determine
        whether a reconcile request has been handled.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: ArubacloudCrossplaneIoV1alpha1BlockstorageStatusAtProvider

    conditions?: [ArubacloudCrossplaneIoV1alpha1BlockstorageStatusConditionsItems0]

    lastHandledReconcileAt?: str

    observedGeneration?: int


schema ArubacloudCrossplaneIoV1alpha1BlockstorageStatusAtProvider:
    r"""
    arubacloud crossplane io v1alpha1 blockstorage status at provider

    Attributes
    ----------
    billingPeriod : str, default is Undefined, optional
        (String) Billing cycle. Accepted values: Hour, Month, Year.
        Billing cycle. Accepted values: `Hour`, `Month`, `Year`.
    bootable : bool, default is Undefined, optional
        (Boolean) Whether this volume can be used as a boot volume for an arubacloud_cloudserver. Must be true when image is set.
        Whether this volume can be used as a boot volume for an `arubacloud_cloudserver`. Must be `true` when `image` is set.
    id : str, default is Undefined, optional
        (String) Computed by the API. Unique identifier for the resource.
    image : str, default is Undefined, optional
        (String) Image ID to use when creating a bootable volume. Required when bootable is true. See the available images.
        Image ID to use when creating a bootable volume. Required when `bootable` is `true`. See the [available images](https://api.arubacloud.com/docs/metadata/#cloud-server-bootvolume).
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the block storage volume.
        Display name for the block storage volume.
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    sizeGb : float, default is Undefined, optional
        created, because the API rejects a smaller size (Validation: Size: invalid). A replacement discards the volume's contents — check the plan before applying.
        Size of the block storage volume in GiB. Must be a positive integer. Growing a volume is applied in place; **shrinking forces the volume to be destroyed and re-created**, because the API rejects a smaller size (`Validation: Size: invalid`). A replacement discards the volume's contents — check the plan before applying.
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    $type : str, default is Undefined, optional
        created.)
        Storage type. Accepted values: `Standard`, `Performance`. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    uri : str, default is Undefined, optional
        (String) Computed by the API. Full resource URI used as a reference value in other resources.
        Computed by the API. Full resource URI used as a reference value in other resources.
    zone : str, default is Undefined, optional
        (String) Availability zone within the region. If omitted the volume is regional (accessible across all zones).
        Availability zone within the region. If omitted the volume is regional (accessible across all zones).
    """


    billingPeriod?: str

    bootable?: bool

    id?: str

    image?: str

    location?: str

    name?: str

    projectId?: str

    sizeGb?: float

    tags?: [str]

    timeout?: str

    $type?: str

    uri?: str

    zone?: str


schema ArubacloudCrossplaneIoV1alpha1BlockstorageStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


