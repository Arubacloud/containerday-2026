"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Securityrule:
    r"""
    Securityrule is the Schema for the Securityrules API. Manages an ArubaCloud Security Rule within an arubacloud_securitygroup.

    Attributes
    ----------
    apiVersion : str, default is "arubacloud.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Securityrule", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ArubacloudCrossplaneIoV1alpha1SecurityruleSpec, default is Undefined, required
        spec
    status : ArubacloudCrossplaneIoV1alpha1SecurityruleStatus, default is Undefined, optional
        status
    """


    apiVersion: "arubacloud.crossplane.io/v1alpha1" = "arubacloud.crossplane.io/v1alpha1"

    kind: "Securityrule" = "Securityrule"

    metadata?: v1.ObjectMeta

    spec: ArubacloudCrossplaneIoV1alpha1SecurityruleSpec

    status?: ArubacloudCrossplaneIoV1alpha1SecurityruleStatus


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpec:
    r"""
    SecurityruleSpec defines the desired state of Securityrule

    Attributes
    ----------
    forProvider : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProvider, default is Undefined, required
        for provider
    initProvider : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProvider

    initProvider?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecProviderConfigRef

    writeConnectionSecretToRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecWriteConnectionSecretToRef


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProvider:
    r"""
    arubacloud crossplane io v1alpha1 securityrule spec for provider

    Attributes
    ----------
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier for the resource (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the security rule.
        Display name for the security rule.
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    projectIdRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    properties : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProperties, default is Undefined, optional
        properties
    securityGroupId : str, default is Undefined, optional
        created.)
        ID of the security group this rule belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    securityGroupIdRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDRef, default is Undefined, optional
        security group Id ref
    securityGroupIdSelector : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDSelector, default is Undefined, optional
        security group Id selector
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    vpcId : str, default is Undefined, optional
        created.)
        ID of the VPC this security rule belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    vpcIdRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDRef, default is Undefined, optional
        vpc Id ref
    vpcIdSelector : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDSelector, default is Undefined, optional
        vpc Id selector
    """


    location?: str

    name?: str

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDSelector

    properties?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProperties

    securityGroupId?: str

    securityGroupIdRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDRef

    securityGroupIdSelector?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDSelector

    tags?: [str]

    timeout?: str

    vpcId?: str

    vpcIdRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDRef

    vpcIdSelector?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDSelector


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderProperties:
    r"""
    matching properties of the security rule. All fields are immutable after creation — to change any of them, destroy and re-create the rule. (see below for nested schema)

    Attributes
    ----------
    direction : str, default is Undefined, optional
        created.)
        Traffic direction the rule applies to. Accepted values: `Ingress`, `Egress`. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    port : str, default is Undefined, optional
        8090). Defaults to * (all ports) when omitted — the API normalises an omitted port to *, so the default keeps plan and state consistent. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Port or port range for TCP/UDP (e.g., `80` or `8080-8090`). Defaults to `*` (all ports) when omitted — the API normalises an omitted port to `*`, so the default keeps plan and state consistent. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    $protocol : str, default is Undefined, optional
        insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
        IP protocol. Accepted values: `TCP`, `UDP`, `ICMP`, `ANY` (case-insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    target : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderPropertiesTarget, default is Undefined, optional
        target
    """


    direction?: str

    port?: str

    $protocol?: str

    target?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderPropertiesTarget


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderPropertiesTarget:
    r"""
    created.) (see below for nested schema)

    Attributes
    ----------
    kind : str, default is Undefined, optional
        insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Type of the target endpoint. Accepted values: `IP`, `SecurityGroup` (case-insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    value : str, default is Undefined, optional
        created.)
        Source (inbound) or destination (outbound) CIDR in notation like `0.0.0.0/0`, or SecurityGroup URI. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    """


    kind?: str

    value?: str


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDRef:
    r"""
    Reference to a Securitygroup in arubacloud to populate securityGroupId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDSelector:
    r"""
    Selector for a Securitygroup in arubacloud to populate securityGroupId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderSecurityGroupIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDRef:
    r"""
    Reference to a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDSelector:
    r"""
    Selector for a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecForProviderVpcIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier for the resource (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the security rule.
        Display name for the security rule.
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    projectIdRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDRef, default is Undefined, optional
        project Id ref
    projectIdSelector : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDSelector, default is Undefined, optional
        project Id selector
    properties : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProperties, default is Undefined, optional
        properties
    securityGroupId : str, default is Undefined, optional
        created.)
        ID of the security group this rule belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    securityGroupIdRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDRef, default is Undefined, optional
        security group Id ref
    securityGroupIdSelector : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDSelector, default is Undefined, optional
        security group Id selector
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    vpcId : str, default is Undefined, optional
        created.)
        ID of the VPC this security rule belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    vpcIdRef : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDRef, default is Undefined, optional
        vpc Id ref
    vpcIdSelector : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDSelector, default is Undefined, optional
        vpc Id selector
    """


    location?: str

    name?: str

    projectId?: str

    projectIdRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDRef

    projectIdSelector?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDSelector

    properties?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProperties

    securityGroupId?: str

    securityGroupIdRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDRef

    securityGroupIdSelector?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDSelector

    tags?: [str]

    timeout?: str

    vpcId?: str

    vpcIdRef?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDRef

    vpcIdSelector?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDSelector


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDRef:
    r"""
    Reference to a Project in arubacloud to populate projectId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDSelector:
    r"""
    Selector for a Project in arubacloud to populate projectId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProjectIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderProperties:
    r"""
    matching properties of the security rule. All fields are immutable after creation — to change any of them, destroy and re-create the rule. (see below for nested schema)

    Attributes
    ----------
    direction : str, default is Undefined, optional
        created.)
        Traffic direction the rule applies to. Accepted values: `Ingress`, `Egress`. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    port : str, default is Undefined, optional
        8090). Defaults to * (all ports) when omitted — the API normalises an omitted port to *, so the default keeps plan and state consistent. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Port or port range for TCP/UDP (e.g., `80` or `8080-8090`). Defaults to `*` (all ports) when omitted — the API normalises an omitted port to `*`, so the default keeps plan and state consistent. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    $protocol : str, default is Undefined, optional
        insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
        IP protocol. Accepted values: `TCP`, `UDP`, `ICMP`, `ANY` (case-insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    target : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderPropertiesTarget, default is Undefined, optional
        target
    """


    direction?: str

    port?: str

    $protocol?: str

    target?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderPropertiesTarget


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderPropertiesTarget:
    r"""
    created.) (see below for nested schema)

    Attributes
    ----------
    kind : str, default is Undefined, optional
        insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Type of the target endpoint. Accepted values: `IP`, `SecurityGroup` (case-insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    value : str, default is Undefined, optional
        created.)
        Source (inbound) or destination (outbound) CIDR in notation like `0.0.0.0/0`, or SecurityGroup URI. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    """


    kind?: str

    value?: str


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDRef:
    r"""
    Reference to a Securitygroup in arubacloud to populate securityGroupId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDSelector:
    r"""
    Selector for a Securitygroup in arubacloud to populate securityGroupId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderSecurityGroupIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDRef:
    r"""
    Reference to a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDRefPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDSelector:
    r"""
    Selector for a VPC in arubacloud to populate vpcId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDSelectorPolicy


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecInitProviderVpcIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema ArubacloudCrossplaneIoV1alpha1SecurityruleSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema ArubacloudCrossplaneIoV1alpha1SecurityruleStatus:
    r"""
    SecurityruleStatus defines the observed state of Securityrule.

    Attributes
    ----------
    atProvider : ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [ArubacloudCrossplaneIoV1alpha1SecurityruleStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    lastHandledReconcileAt : str, default is Undefined, optional
        LastHandledReconcileAt holds the value of the most recent
        reconcile-requested-at annotation token that the controller has
        processed. Users can compare this to the annotation to determine
        whether a reconcile request has been handled.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProvider

    conditions?: [ArubacloudCrossplaneIoV1alpha1SecurityruleStatusConditionsItems0]

    lastHandledReconcileAt?: str

    observedGeneration?: int


schema ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProvider:
    r"""
    arubacloud crossplane io v1alpha1 securityrule status at provider

    Attributes
    ----------
    id : str, default is Undefined, optional
        (String) Computed by the API. Unique identifier for the resource.
    location : str, default is Undefined, optional
        Bergamo). See the available locations and zones. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Region identifier for the resource (e.g., `ITBG-Bergamo`). See the [available locations and zones](https://api.arubacloud.com/docs/metadata/#location-and-data-center). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    name : str, default is Undefined, optional
        (String) Display name for the security rule.
        Display name for the security rule.
    projectId : str, default is Undefined, optional
        created.)
        ID of the project that owns this resource. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    properties : ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProviderProperties, default is Undefined, optional
        properties
    securityGroupId : str, default is Undefined, optional
        created.)
        ID of the security group this rule belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    tags : [str], default is Undefined, optional
        (List of String) List of string tags attached to the resource for filtering and organisation.
        List of string tags attached to the resource for filtering and organisation.
    timeout : str, default is Undefined, optional
        resource timeout override (e.g. "15m", "1h"). Overrides the provider-level resource_timeout for this resource's Create and Delete operations. Uses Go duration syntax.
        Per-resource timeout override (e.g. `"15m"`, `"1h"`). Overrides the provider-level `resource_timeout` for this resource's Create and Delete operations. Uses Go duration syntax.
    uri : str, default is Undefined, optional
        (String) Computed by the API. Full resource URI used as a reference value in other resources (e.g., as a *_uri_ref attribute).
        Computed by the API. Full resource URI used as a reference value in other resources (e.g., as a `*_uri_ref` attribute).
    vpcId : str, default is Undefined, optional
        created.)
        ID of the VPC this security rule belongs to. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    """


    id?: str

    location?: str

    name?: str

    projectId?: str

    properties?: ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProviderProperties

    securityGroupId?: str

    tags?: [str]

    timeout?: str

    uri?: str

    vpcId?: str


schema ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProviderProperties:
    r"""
    matching properties of the security rule. All fields are immutable after creation — to change any of them, destroy and re-create the rule. (see below for nested schema)

    Attributes
    ----------
    direction : str, default is Undefined, optional
        created.)
        Traffic direction the rule applies to. Accepted values: `Ingress`, `Egress`. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    port : str, default is Undefined, optional
        8090). Defaults to * (all ports) when omitted — the API normalises an omitted port to *, so the default keeps plan and state consistent. (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Port or port range for TCP/UDP (e.g., `80` or `8080-8090`). Defaults to `*` (all ports) when omitted — the API normalises an omitted port to `*`, so the default keeps plan and state consistent. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    $protocol : str, default is Undefined, optional
        insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
        IP protocol. Accepted values: `TCP`, `UDP`, `ICMP`, `ANY` (case-insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    target : ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProviderPropertiesTarget, default is Undefined, optional
        target
    """


    direction?: str

    port?: str

    $protocol?: str

    target?: ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProviderPropertiesTarget


schema ArubacloudCrossplaneIoV1alpha1SecurityruleStatusAtProviderPropertiesTarget:
    r"""
    created.) (see below for nested schema)

    Attributes
    ----------
    kind : str, default is Undefined, optional
        insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
        Type of the target endpoint. Accepted values: `IP`, `SecurityGroup` (case-insensitive — the value is normalised before sending to the API). (Immutable — changing this value forces the resource to be destroyed and re-created.)
    value : str, default is Undefined, optional
        created.)
        Source (inbound) or destination (outbound) CIDR in notation like `0.0.0.0/0`, or SecurityGroup URI. (Immutable — changing this value forces the resource to be destroyed and re-created.)
    """


    kind?: str

    value?: str


schema ArubacloudCrossplaneIoV1alpha1SecurityruleStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


